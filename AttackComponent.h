#ifndef _ATTACKC_H                 // Prevent multiple definitions if this 
#define _ATTACKC_H                 // file is included in more than one place
#define WIN32_LEAN_AND_MEAN

class AttackComponent
{
private:
	

public:
	void update(float frameTime);
};
#endif
